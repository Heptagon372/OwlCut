# 아울네컷 (S.OWL PHOTO BOOTH) 개발 설계도

> 원본 명세서(S.OWL PHOTO BOOTH 개발 명세서)를 실제 구현 가능한 아키텍처로 구체화한 문서.
> Phase 1(카메라 MVP)부터 순서대로 작업.

---

## 1. 시스템 아키텍처 개요

```text
┌─────────────────────────────────────────────┐
│                Client (Browser)               │
│  Next.js App Router / React / TypeScript      │
│                                               │
│  Camera → Tracking → Editor → AI → Result     │
└───────────────┬───────────────────────────────┘
                │ REST / Server Action
┌───────────────▼───────────────────────────────┐
│         Next.js API Route (Backend)           │
│  /api/session  /api/photo  /api/design        │
│  /api/ai       /api/qr     /api/print         │
└───────┬──────────────┬────────────┬───────────┘
        │              │            │
┌───────▼───────┐ ┌────▼─────┐ ┌────▼─────────┐
│  PostgreSQL   │ │ Storage  │ │  Print Server │
│ (Supabase)    │ │ (S3/R2/  │ │  (별도 프로세스│
│               │ │ Supabase)│ │  or 로컬 서비스)│
└───────────────┘ └──────────┘ └───────────────┘
```

- Frontend/Backend 통합: 초기엔 Next.js 하나로. Print Server만 별도 로컬 프로세스(행사장 PC에서 프린터 직결)로 분리.
- Storage/DB: Supabase 하나로 통일 추천 (Postgres + Storage 동시 제공, 기존에 쓰고 있으니 재사용 가능).

---

## 2. 기술 스택 확정

| 영역 | 선택 |
|---|---|
| Frontend | Next.js 15 (App Router) + TypeScript |
| 스타일 | Tailwind CSS |
| 카메라 | `getUserMedia()` (WebRTC MediaDevices API) |
| 사람 추적 | MediaPipe Tasks Vision (Face/Pose Landmarker) — Phase 6에서 도입 |
| 이미지 합성 | Canvas API 기본, 복잡해지면 Konva.js 도입 |
| DB/Storage | Supabase (Postgres + Storage) |
| QR | `qrcode` (npm) |
| AI | 멀티 프로바이더 (Claude / OpenAI / Gemini) — 어댑터로 추상화, 화면에서 모델 선택 |
| 배포/CI | GitHub + GitHub Actions → Vercel(권장) 또는 AWS 자동 배포 |
| 인증 없음 | 관리자 페이지는 간단한 비밀번호/토큰 게이트만 |

---

## 3. 디렉토리 구조 (확정판)

```text
sowl-photo-booth/
├── app/
│   ├── page.tsx                  # 시작 화면
│   ├── camera/page.tsx           # 촬영 화면
│   ├── edit/page.tsx             # 수동/AI 꾸미기 선택+편집
│   ├── result/page.tsx           # QR + 다운로드 + 출력
│   ├── download/[id]/page.tsx    # QR로 접속하는 모바일 다운로드 페이지
│   ├── admin/page.tsx            # 관리자 대시보드
│   └── api/
│       ├── session/route.ts      # POST 세션 생성, GET 조회
│       ├── photo/route.ts        # 사진 업로드
│       ├── design/route.ts       # 편집 결과 저장
│       ├── ai/route.ts           # 프롬프트 → 디자인 JSON
│       ├── final/route.ts        # 최종 이미지 업로드 + URL 발급
│       ├── qr/route.ts           # QR 이미지 생성
│       └── print/route.ts        # 출력 큐 등록
│
├── components/
│   ├── camera/ (CameraView, Countdown, TrackingOverlay)
│   ├── editor/ (PhotoCanvas, FrameSelector, StickerPanel, TextEditor)
│   ├── ai/ (PromptInput, AIDesignPreview, ModelSelector)   # ← 모델 선택 창
│   └── result/ (QRCode, PrintButton)
│
├── lib/
│   ├── camera/useCamera.ts
│   ├── tracking/useFaceTracking.ts
│   ├── ai/
│   │   ├── generateDesign.ts      # 프롬프트 → 디자인 JSON (프로바이더 무관)
│   │   ├── providers/
│   │   │   ├── types.ts           # AIProvider 인터페이스
│   │   │   ├── claude.ts
│   │   │   ├── openai.ts
│   │   │   └── gemini.ts
│   │   ├── registry.ts            # 사용 가능한 모델 목록 정의
│   │   └── prompt.ts              # 공통 system prompt
│   ├── image/compose.ts           # Canvas 합성 엔진 (핵심 모듈)
│   ├── qr/generateQr.ts
│   ├── printer/printQueue.ts
│   └── supabase/client.ts
│
├── data/
│   ├── frames/*.json
│   ├── stickers/*.json
│   └── layouts/*.json
│
├── scripts/
│   └── auto-push.sh               # 로컬 자동 커밋/푸시 스크립트
│
├── .github/
│   └── workflows/
│       ├── ci.yml                 # lint + typecheck + build
│       └── deploy.yml             # main 푸시 시 자동 배포
│
└── types/
    ├── session.ts
    ├── design.ts
    └── print.ts
```

---

## 4. 데이터베이스 스키마 (Supabase / Postgres)

```sql
create table sessions (
  id uuid primary key default gen_random_uuid(),
  status text not null default 'created', -- created|capturing|editing|done|expired
  created_at timestamptz default now(),
  expires_at timestamptz default (now() + interval '2 hours')
);

create table photos (
  id uuid primary key default gen_random_uuid(),
  session_id uuid references sessions(id) on delete cascade,
  image_url text not null,
  order_index int not null,
  created_at timestamptz default now()
);

create table designs (
  id uuid primary key default gen_random_uuid(),
  session_id uuid references sessions(id) on delete cascade,
  mode text not null, -- manual|ai
  prompt text,
  frame text,
  stickers jsonb,
  text_layers jsonb,
  filter text,
  final_image_url text,
  created_at timestamptz default now()
);

create table prints (
  id uuid primary key default gen_random_uuid(),
  session_id uuid references sessions(id) on delete cascade,
  printer text,
  status text not null default 'waiting', -- waiting|printing|completed|failed
  created_at timestamptz default now()
);
```

- TTL: `expires_at` 지난 세션은 크론(Supabase Edge Function 또는 Vercel Cron)으로 photos/designs 스토리지 파일까지 같이 삭제.

---

## 5. 세션 상태 머신

```text
created → capturing → captured → editing(manual|ai) → composed → qr_issued → (printed) → expired
```

- 각 단계 실패 시 이전 단계로 롤백 가능해야 함 (재촬영, 재편집).
- `status`는 sessions 테이블에 저장, 프론트는 이 값 기준으로 라우팅.

---

## 6. API 설계

| Method | Path | 역할 |
|---|---|---|
| POST | `/api/session` | 세션 생성 → session_id 반환 |
| POST | `/api/photo` | 사진 1장 업로드 (session_id, order_index, file) |
| GET | `/api/ai/models` | 선택 가능한 AI 모델 목록 반환 (모델 선택 창용) |
| POST | `/api/ai` | { session_id, prompt, model } → 디자인 JSON 반환 |
| POST | `/api/design` | 수동/AI 디자인 결과 저장 (frame/stickers/text/filter) |
| POST | `/api/final` | 합성된 최종 이미지 업로드 → final_image_url |
| GET | `/api/qr?session_id=` | QR 이미지(PNG) 반환 |
| GET | `/download/[id]` | 모바일 다운로드 페이지 (SSR) |
| POST | `/api/print` | 출력 큐 등록 |
| GET | `/api/admin/stats` | 관리자 대시보드 통계 |

---

## 7. 핵심 모듈 설계

### 7-1. 카메라 모듈 (`lib/camera/useCamera.ts`)
- `navigator.mediaDevices.getUserMedia({video: true})`
- 권한 거부/장치 없음 예외 처리 필수 → UI에 명확한 에러 메시지
- `<video>` → `<canvas>` 캡처 → Blob 변환 → 업로드

### 7-2. 사람 추적 모듈 (Phase 6)
- MediaPipe Face Landmarker로 얼굴 bounding box 추출
- 중앙 좌표와 프레임 중앙 비교 → CSS `object-position` 또는 canvas crop 좌표 조정
- 실패해도 촬영 자체는 되게 (추적은 보조 기능, 필수 아님)

### 7-3. 이미지 합성 엔진 (`lib/image/compose.ts`) — 가장 중요
```ts
compose({
  photos: string[4],     // 사진 URL
  layout: LayoutConfig,  // data/layouts/*.json
  frame: FrameConfig,    // data/frames/*.json
  stickers: StickerInstance[],
  textLayers: TextLayer[],
  filter: FilterName
}) => Promise<Blob>
```
- 레이어 순서: Background → Frame → Photos → Stickers → Text → Effects
- 수동 편집과 AI 편집이 **동일한 엔진**을 공유 (AI는 이 함수의 입력 JSON만 만들어줌)

### 7-4. AI 디자인 모듈 (멀티 프로바이더 + 모델 선택)

**핵심 아이디어:** AI는 이미지를 그리지 않고 "디자인 명령 JSON"만 만든다. 어떤 모델을 쓰든 출력 스키마는 동일 → 프로바이더를 자유롭게 갈아끼움.

**어댑터 인터페이스 (`lib/ai/providers/types.ts`)**
```ts
export interface AIProvider {
  id: string;                       // "claude-3-5-sonnet" 등
  generate(prompt: string, system: string): Promise<string>; // raw text 반환
}
```
- `claude.ts`, `openai.ts`, `gemini.ts`가 각각 이 인터페이스를 구현.
- `generateDesign.ts`는 프로바이더를 몰라도 되고, 받은 raw text를 파싱/검증만 함.

**모델 레지스트리 (`lib/ai/registry.ts`)**
```ts
export const MODELS = [
  { id: "claude-3-5-sonnet", label: "Claude 3.5 Sonnet", provider: "claude", default: true },
  { id: "gpt-4o",            label: "GPT-4o",            provider: "openai" },
  { id: "gemini-1.5-pro",    label: "Gemini 1.5 Pro",    provider: "gemini" },
];
// API 키가 세팅된 프로바이더만 노출 (env 체크)
```
> 실제 모델 ID/이름은 개발 시점 최신으로 갱신. 여기선 구조만 확정.

**모델 선택 UI (`components/ai/ModelSelector.tsx`)**
```text
🤖 AI PHOTO DESIGNER

사용할 AI 모델
┌───────────────────────────┐
│ ● Claude 3.5 Sonnet   (기본)│
│ ○ GPT-4o                   │
│ ○ Gemini 1.5 Pro           │
└───────────────────────────┘

원하는 분위기를 입력하세요.
┌───────────────────────────┐
│ 보라색 사이버펑크 느낌으로 │
└───────────────────────────┘
        [ AI 꾸미기 ]
```
- 페이지 로드 시 `GET /api/ai/models`로 사용 가능한 모델만 받아와 렌더.
- 선택값은 세션 state에 저장 → `POST /api/ai`에 `model` 필드로 전달.
- 관리자 페이지에서 기본 모델/노출 모델을 토글할 수 있게 확장 여지.

**요청 처리 흐름**
```text
POST /api/ai { prompt, model }
   ↓
registry에서 model → provider 매핑
   ↓
provider.generate(prompt, systemPrompt)
   ↓
raw text → JSON 파싱
   ↓
frame/sticker id 검증 (data/* 에 존재하나?)
   ↓
없으면 기본값 치환, 파싱 실패면 basic 프레임 fallback
   ↓
디자인 JSON 반환
```

**응답 스키마 (모델 공통)**
```json
{
  "frame": "cyber_purple",
  "stickers": [{ "type": "owl", "position": "top-right" }],
  "text": { "content": "S.OWL 2026", "position": "bottom" },
  "filter": "cool"
}
```
- 어떤 모델이 실패해도 다른 모델/수동 편집으로 넘어갈 수 있어야 함 (AI는 보조 기능).
- env에 키 없는 프로바이더는 목록에서 아예 숨김 → 선택 불가로 오류 원천 차단.

### 7-5. QR/다운로드 모듈
- 최종 이미지는 Supabase Storage에 업로드 → public URL 또는 signed URL
- QR에는 이미지 자체가 아니라 `https://photo.sowl.kr/download/{session_id}` 인코딩
- 다운로드 페이지는 SSR로 미리보기 + 다운로드 버튼만 (가볍게)

### 7-6. 프린터 모듈
- Web App → `/api/print`로 큐 등록만 하고 즉시 응답
- 실제 프린터 제어는 행사장 PC에서 도는 별도 로컬 프린트 서버(Node 상시 프로세스)가 큐를 폴링해서 처리
- 프린터 미연결/오류 시에도 웹사이트 흐름은 절대 막히지 않게 (`status: failed` 로만 표시)

---

## 8. 자동 GitHub 업로드 & CI/CD 배포

### 8-0. 흐름 요약
```text
로컬 코드 수정
   ↓
auto-push.sh (자동 커밋 + 푸시)     ← 선택: 수동 git push 해도 됨
   ↓
GitHub (main 브랜치)
   ↓
GitHub Actions
   ├── ci.yml    : lint + typecheck + build 검증
   └── deploy.yml: 검증 통과 시 자동 배포
   ↓
Vercel(권장) 또는 AWS
   ↓
https://photo.sowl.kr 반영
```

### 8-1. 저장소 초기 세팅
```bash
git init
git branch -M main
git remote add origin https://github.com/S-OWL/sowl-photo-booth.git
# .env, node_modules, 촬영 임시파일은 .gitignore 에 반드시 포함
git add . && git commit -m "init" && git push -u origin main
```
`.gitignore` 필수 항목: `.env*`, `node_modules`, `.next`, `/public/tmp`, `*.local`

### 8-2. 로컬 자동 커밋/푸시 (`scripts/auto-push.sh`)
```bash
#!/usr/bin/env bash
# 사용: ./scripts/auto-push.sh "커밋 메시지"
set -e
MSG=${1:-"chore: auto update $(date '+%Y-%m-%d %H:%M')"}
git add -A
# 변경 없으면 조용히 종료
git diff --cached --quiet && echo "변경 없음" && exit 0
git commit -m "$MSG"
git push origin main
echo "✅ 푸시 완료"
```
- Windows면 `auto-push.ps1`(PowerShell)이나 npm 스크립트(`"push": "git add -A && git commit -m ... && git push"`)로도 가능.
- 저장 시 자동 실행하고 싶으면 VS Code 확장(Git Auto Commit) 또는 watch 스크립트 연결. 단, 자동 푸시는 실수 커밋 위험 있으니 처음엔 수동 실행 권장.

### 8-3. CI 워크플로 (`.github/workflows/ci.yml`)
```yaml
name: CI
on:
  push: { branches: [main] }
  pull_request: { branches: [main] }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm run build
```

### 8-4. 자동 배포 (`.github/workflows/deploy.yml`)

**옵션 A — Vercel (권장, Next.js 최적)**
- GitHub 저장소를 Vercel에 Import 하면 별도 워크플로 없이 main 푸시마다 자동 배포됨 (가장 간단).
- 환경변수(Supabase 키, AI API 키)는 Vercel 대시보드에 등록.
- 커스텀 도메인 `photo.sowl.kr` 연결.

**옵션 B — AWS 자동 배포 (기존 인프라 재사용 시)**
```yaml
name: Deploy
on:
  push: { branches: [main] }
jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []   # ci 통과 후로 묶으려면 workflow_run 사용
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci && npm run build
      # 예: S3 + CloudFront 정적 호스팅이면 sync, 서버형이면 EC2/ECS 배포 스텝
      - name: Deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          echo "AWS 배포 스텝 (S3 sync / EC2 pull / ECS update 중 택1)"
```

### 8-5. 시크릿 관리
- 절대 코드/커밋에 키 넣지 않기. GitHub → Settings → Secrets에 등록:
  `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GEMINI_API_KEY`, (AWS면) `AWS_*`.
- 로컬은 `.env.local`, 배포는 플랫폼 환경변수. `.env`는 `.gitignore`.

---

## 9. Phase별 작업 체크리스트

### Phase 0 — 저장소 & 자동 배포 세팅
- [ ] GitHub 저장소 생성 + `.gitignore`
- [ ] `ci.yml` (lint/typecheck/build)
- [ ] Vercel Import 또는 AWS deploy.yml → main 푸시 시 자동 배포 확인
- [ ] 시크릿 등록 (Supabase / AI 키)

### Phase 1 — Camera MVP
- [ ] Next.js + TS 프로젝트 초기화
- [ ] `/camera` 라우트 + getUserMedia 연결
- [ ] 3초 카운트다운 컴포넌트
- [ ] 4장 연속 촬영 + 로컬 state 저장
- [ ] 촬영 후 4장 미리보기 + 재촬영 버튼

### Phase 2 — 인생네컷 생성
- [ ] `compose()` 초기 버전 (기본 레이아웃 1종)
- [ ] PNG export + 즉시 다운로드

### Phase 3 — 수동 편집
- [ ] FrameSelector, StickerPanel, TextEditor
- [ ] data/frames, data/stickers JSON 스키마 확정

### Phase 4 — QR
- [ ] Supabase Storage 업로드
- [ ] sessions/photos/designs 테이블 연동
- [ ] `/download/[id]` 페이지

### Phase 5 — AI (멀티 모델)
- [ ] provider 어댑터(claude/openai/gemini) + registry
- [ ] `/api/ai/models` + `ModelSelector` UI (모델 선택 창)
- [ ] `/api/ai` 라우트 (model 파라미터 처리)
- [ ] JSON 검증/fallback 로직 (파싱 실패 시 basic 프레임)

### Phase 6 — 사람 추적
- [ ] MediaPipe 연동, auto framing overlay

### Phase 7 — Printer
- [ ] 로컬 Print Server 스캐폴딩
- [ ] `/api/print` 큐 연동

### Phase 8 — Admin
- [ ] `/admin` 대시보드 (세션 수, AI 사용량, 출력 완료, 장비 상태)

---

## 10. 에러 처리 원칙 (요약)

| 실패 지점 | 대응 |
|---|---|
| 카메라 접근 실패 | 권한 안내 화면, 재시도 버튼 |
| AI 응답 파싱 실패 | 기본 프레임으로 fallback, 수동 편집 유도 |
| 특정 AI 모델 오류/한도 초과 | 다른 모델 선택 유도 or 기본 모델로 재시도, 키 없는 모델은 목록서 숨김 |
| Storage 업로드 실패 | 재시도 3회 → 실패 시 로컬 다운로드만 제공 |
| 프린터 오류 | 큐 상태 `failed`, 사용자에겐 QR/다운로드는 정상 제공 |
| 네트워크 끊김 | 로컬 캡처 데이터는 유지, 재연결 시 자동 재업로드 |

---

## 11. 향후 확장 고려사항
- 촬영 매수 4→6/8장 (layout JSON만 추가하면 대응되도록 설계됨)
- PTZ 카메라로 실제 하드웨어 프레이밍
- AI 디자인 여러 개 생성 후 선택 (결과 A/B/C) — 모델별로 A/B 비교도 가능
- 새 AI 프로바이더 추가는 어댑터 1개(`providers/xxx.ts`) + registry 한 줄로 끝
- 모델별 사용량/비용 관리자 대시보드 집계
- 다국어 UI (행사 특성상 필요 시)
